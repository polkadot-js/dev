language: node_js
node_js:
  - "10"
cache:
  yarn: true
  directories:
    - node_modules
before_install:
  - npm install --global yarn --cache-min 999999999
install:
  - yarn install
script:
  - ./scripts/polkadot-dev-build-travis.sh
