{
  "version": "0.19.24",
  "private": true,
  "engines": {
    "node": "^10.1.0",
    "yarn": "^1.3.2"
  },
  "workspaces": [
    "dev",
    "dev-react"
  ],
  "scripts": {
    "build": "polkadot-dev-build-docs && polkadot-dev-build-babel && (cd dev && copyfiles config/* flow-typed/* scripts/* skeleton/* build) && (cd dev-react && copyfiles config/* flow-typed/* scripts/* skeleton/* build)",
    "check": "polkadot-dev-lint-js && polkadot-dev-lint-css",
    "clean": "polkadot-dev-clean-build",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "copyfiles": "^2.0.0"
  }
}
