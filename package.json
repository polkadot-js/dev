{
  "version": "0.18.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "./packages/dev/scripts/polkadot-dev-build-docs.js && ./packages/dev/scripts/polkadot-dev-build-babel.sh && ./packages/dev/scripts/polkadot-dev-clean-build.sh && (cd packages/dev && copyfiles config/* flow-typed/* scripts/* skeleton/* build) && (cd packages/dev-react && copyfiles config/* flow-typed/* scripts/* skeleton/* build)",
    "check": "eslint packages && flow check",
    "clean": "polkadot-dev-clean-build",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "copyfiles": "^2.0.0",
    "lerna": "^2.11.0"
  }
}
