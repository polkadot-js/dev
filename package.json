{
  "version": "0.40.17",
  "private": true,
  "engines": {
    "yarn": "^1.10.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "resolutions": {
    "typescript": "^3.7.5"
  },
  "scripts": {
    "build": "polkadot-dev-build-ts && (cd packages/dev && copyfiles config/* scripts/* skeleton/* build)",
    "docs": "polkadot-dev-build-docs",
    "lint": "polkadot-dev-run-lint",
    "clean": "polkadot-dev-clean-build",
    "postinstall": "./packages/dev/scripts/polkadot-dev-yarn-only.js",
    "test": "polkadot-dev-run-test --coverage"
  },
  "devDependencies": {
    "copyfiles": "^2.2.0"
  }
}
